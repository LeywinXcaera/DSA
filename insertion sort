# INSERTION SORT
a = [10, 9, 7, 101, 23, 44, 12, 78, 34, 23]
print("\ninsertion_sort\n")

for k in range(1, len(a)):
    temp = a[k]
    j = k - 1

    while j >= 0 and temp <= a[j]:
        a[j + 1] = a[j]
        j = j - 1

    a[j + 1] = temp

for i in range(10):
    print(a[i])
